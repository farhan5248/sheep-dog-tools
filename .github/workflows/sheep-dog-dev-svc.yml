name: Publish Sheep Dog Dev Service Release

on:
  workflow_run:
    workflows: ["Publish Sheep Dog Dev Maven Plugin Release"]
    types:
      - completed
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - 'sheep-dog-dev-svc/**'
permissions:
  contents: write
  packages: write

jobs:
  sheep-dog-dev-svc:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/release.yml
    with:
      project: 'sheep-dog-dev-svc'
    secrets: inherit