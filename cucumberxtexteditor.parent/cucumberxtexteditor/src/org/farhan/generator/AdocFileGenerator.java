package org.farhan.generator;

public class AdocFileGenerator {

	public static CharSequence getContent(String fileName, String component, String object, String stepName,
			String stepTable) {

		System.out.println("Generating content for: " + stepName);
		// Search for the file
		// If it doesn't exist, create it but don't write it out
		// Else read it in

		// Search for the step
		// If it doesn't exist, create it.

		// Check for a table
		// If it doesn't exist, create it.
		if (!stepTable.isEmpty()) {
		}
		return new StringBuilder();
	}

}
